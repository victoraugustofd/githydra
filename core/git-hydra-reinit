
reinit() {
    list_hydra_config
    info "Are you sure that you want to reinit Hydra? (y/n) "
    read p_confirm

    if [ ${p_confirm,,} = "y" ]; then
        destroy "confirmed"
        init
    fi
}
